# JobPulse

React/Vite version of the JobPulse employment platform recovered from the preview deployment.

## Run locally

```bash
npm install
npm run dev
```

Set `VITE_BACKEND_URL` if the API is hosted on a different origin. When it is empty, API calls use `/api`.
